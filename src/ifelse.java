import javax.swing.*;

public class ifelse {
    public static void main(String[] args) {

        String name1 = JOptionPane.showInputDialog("Please enter the first name: ");
        String name2 = JOptionPane.showInputDialog("Please enter the second name: ");
        /*
        int x;
        int y;
        int z;
        String input;

        input = JOptionPane.showInputDialog("This Program will compare 3 words you type. So please enter a first number!");
        x = Integer.parseInt(input);
        input = JOptionPane.showInputDialog("Please enter a second number!");
        y = Integer.parseInt(input);
        input = JOptionPane.showInputDialog("Please enter a third number!");
        z = Integer.parseInt(input);
        String choosex = "the first number you chose which is " + x;
        String choosey = "the second number you chose which is " + y;
        String choosez = "the third number you chose which is " + z;
        boolean xequalsy = x == y;
        boolean xnoteequalsy = x != y;
        boolean xequalsz = x == z;
        boolean xnotequalsz = x != z;
        boolean yequalsz = y == z;
        boolean ynotequalsz = y != z;

        if (xequalsy) {
            JOptionPane.showMessageDialog(null, choosex + " and " + choosez + " are the same!");
            }
        else if (xequalsy && !(xequalsz)) {
            JOptionPane.showMessageDialog(null, choosex + " is equal to " + choosey + ", but is not equal to " + choosez);
        }
        else if (xequalsy && !(xequalsy)) {
            JOptionPane.showMessageDialog(null, choosex + " is equal to " + choosez + ", but is not equal to " + choosey);
        }
        if (name1.equals(name2)) {
            JOptionPane.showMessageDialog(null,"They are the same!");
        }
        else {
            JOptionPane.showMessageDialog(null, "They are not the same!" );
        } */







        System.exit(0);
    }
}
